# Global guidance for coding agents

- Follow repository-local `AGENTS.md` files and existing project conventions.
- Do not use em dashes (`—`) in prose. Use commas, parentheses, colons, or separate sentences instead.
